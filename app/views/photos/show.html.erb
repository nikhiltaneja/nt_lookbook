<div class="row">
  <div class="col-xs-2">
    <% unless @featured_photo == @photos.last %>
      <%= link_to photo_path(@featured_photo.id + 1) do %>
        <span class="glyphicon glyphicon-chevron-left arrow-left pull-right"></span>
      <% end %>
    <% end %>
  </div>

  <div class="col-xs-8">
    <%= image_tag @featured_photo.standard_url, class:'img-responsive featured-photo', id: @featured_photo.id %>
  </div>

  <div class="col-xs-2">
    <% unless @featured_photo == @photos.first %>
      <%= link_to photo_path(@featured_photo.id - 1) do %>
        <span class="glyphicon glyphicon-chevron-right arrow-right pull-left"></span>
      <% end %>
    <% end %>
  </div>
</div>

<div class="row">
  <div>
    <% @photos.reverse.each do |photo| %>
      <% if @featured_photo == photo %>
        <%= link_to photo_path(photo.id) do %>
          <%= image_tag photo.thumbnail_url, class:'photo selected-photo', id: photo.id %>
        <% end %>
      <% else %>
        <%= link_to photo_path(photo.id) do %>
          <%= image_tag photo.thumbnail_url, class:'photo img-thumbnail', id: photo.id %>
        <% end %>
      <% end %>
    <% end %>
    </div>
  </div>
</div>
